
# 零基础数据探索：用 Python 玩转你的数据集

---

## 一、 核心概念：数据、数据集与我们的世界

### 1. 什么是数据集？
* **数据集 (Dataset)**：按照特定规则整理好的海量数据集合。在计算机中，它通常表现为一张结构整齐的表格。

### 2. 数据集的应用场景
* **医疗健康**：通过分析海量的心率与睡眠数据集，AI 可以提前识别潜在的健康风险。
* **个性化推荐**：音乐或购物软件分析你的历史行为数据集，从而为你精准推送感兴趣的内容。
* **自动驾驶**：汽车通过学习海量的道路交通图像数据集，学会如何避开障碍物。

### 3. 为什么要学习使用数据集？
在人工智能时代，**数据是喂给 AI的燃料**。掌握数据集的使用方法，意味着你能够从凌乱的数字中发现规律，并利用这些规律解决实际问题，比如优化智能健康监测设备的算法。

---

## 二、 准备工作：连接 Python 与你的数据集

### 1. 工具引入：认识 Pandas
处理数据集文件（如 `.csv` 或 `.xlsx`），我们使用 Python 的 **Pandas** 库。它就像是 Excel 的超级增强版，但可以用代码自动处理成千上万行数据。

**Pandas 的核心优势**：
- 📊 直接读取 CSV/Excel 等常见文件格式
- ⚡ 快速处理海量数据（10万行也能秒级响应）
- 🔧 提供丰富的数据分析函数
- 📈 完美配合数据可视化工具

### 2. 教学数据集预览
我们将使用一份虚拟的智能戒指健康监测数据 `smart_ring_health.csv`。这份数据模拟了真实的可穿戴设备记录：

| 日期 | 用户ID | 心率(bpm) | 睡眠(小时) | 步数 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-03-01 | U_001 | 75 | 8.2 | 8500 |
| 2026-03-02 | U_001 | 82 | 6.5 | 12000 |
| 2026-03-03 | U_002 | 68 | 9.0 | 5400 |

**数据字段说明**：
- **日期**：记录时间
- **用户ID**：不同用户的唯一标识
- **心率(bpm)**：每分钟心跳次数（正常范围：60-100）
- **睡眠(小时)**：总睡眠时长
- **步数**：全天行走步数

### 3. 读取数据集
使用 `pd.read_csv()` 将外部文件读取为 Python 中的 `DataFrame` 对象（类似 Excel 表格）：

```python
# 导入 pandas 库
import pandas as pd

# 读取 CSV 数据集
# 确保文件与 Python 脚本在同一文件夹
df = pd.read_csv("smart_ring_health.csv")

# 查看数据的前 3 行
print("--- 数据预览 ---")
print(df.head(3))

# 查看数据集基本信息
print("\n--- 数据集信息 ---")
print(f"总行数：{len(df)}")
print(f"列名：{list(df.columns)}")

```

---

## 三、 数据魔法：基本的增、删、改

在获取数据后，我们通常需要对其进行加工处理。

### 1. 增加数据 (增 - Create)

**应用场景**：我们有“步数”，现在想计算用户消耗的热量。
**语法讲解**：通过 `df['新列名'] = 计算公式` 即可完成批量计算。

```python
# 增加一列：假设每走 1 步消耗 0.04 千卡
df['消耗卡路里'] = df['步数'] * 0.04

print("\n--- 增加‘消耗卡路里’列后的结果 ---")
print(df[['步数', '消耗卡路里']].head())

```

### 2. 修改数据 (改 - Update)

**应用场景**：根据心率高低，自动为用户打上健康状态标签。
**语法讲解**：使用 `df.loc[条件, '列名'] = 新值` 进行条件修改。

```python
# 修改数据：将心率大于 80 的记录标记为'偏高'，其余为'正常'
df.loc[df['心率(bpm)'] > 80, '健康状态'] = '偏高'
df.loc[df['心率(bpm)'] <= 80, '健康状态'] = '正常'

print("\n--- 标记健康状态后的结果 ---")
print(df[['心率(bpm)', '健康状态']].head())

```

### 3. 删除数据 (删 - Delete)

**应用场景**：移除数据集中不需要的列（如内部 ID），让数据更清爽。
**语法讲解**：使用 `df.drop(columns=['列名'])` 移除特定列。

```python
# 删除 '用户ID' 列 (axis=1 代表操作列)
df_cleaned = df.drop(columns=['用户ID'])

print("\n--- 删除冗余列后的结果 ---")
print(df_cleaned.head())

```

---
## 四、 互动实战区

**练习任务：**
1. 运行上方的所有示例代码，观察控制台输出的结果。
2. **动手尝试**：在下方的编辑区编写代码，将“睡眠(小时)”数据转换为“睡眠(分钟)”，并存入新的一列。

```python
import pandas as pd

# 加载数据集
# 注意：请确保目录下已存在 smart_ring_health.csv 文件
df = pd.read_csv("smart_ring_health.csv")

# --- 【练习答案】在此编写你的代码 ---
# 逻辑：睡眠(分钟) = 睡眠(小时) * 60
df['睡眠(分钟)'] = df['睡眠(小时)'] * 60

# --- 验证代码（自动运行）---
print("--- 转换结果预览 ---")
# 选取原列与新生成的列进行对比查看
print(df[['睡眠(小时)', '睡眠(分钟)']].head())

print(f"\n数据集处理成功！当前总记录数：{len(df)}")
```
